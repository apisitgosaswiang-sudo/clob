# Morning Warrior v3 — Batch 1

Cache version: `morning-warrior-v3-batch1`

---

## 🔴 Tier 1 — ข้อมูลไม่ถึงปลายทาง / เสี่ยงสูญหาย

### 1. คำติชมของเทรนเนอร์ถึงลูกเทรนแล้ว
**เดิม:** โค้ชเขียน feedback / เป้าหมายสัปดาห์หน้า / ปรับ macros ทุกสัปดาห์ → บันทึกลง Firebase แต่ **ลูกเทรนไม่เห็นเลยแม้แต่ตัวอักษรเดียว** คำถามที่ลูกเทรนถามไปก็ไม่มีวันเห็นคำตอบ

**แก้แล้ว:**
- เขียนหน้า Weekly Update ฝั่งลูกเทรนใหม่ทั้งไฟล์ — แสดงคำติชม, เป้าหมายสัปดาห์หน้า, การปรับโปรแกรมเทรน, เป้าโภชนาการที่โค้ชปรับให้
- ถ้าส่งแล้วรอรีวิว → แสดง "โค้ชกำลังตรวจรายงานของคุณ"
- **Banner แจ้งเตือนบนหน้า Home** เมื่อมีรีวิวใหม่ที่ยังไม่เคยเปิดดู (จำว่าอ่านแล้วผ่าน `clob_seen_review_{code}`)

### 2. ตัวเลขโภชนาการในรีวิวถูกนำไปใช้จริงแล้ว
**เดิม:** โค้ชกรอก Calories/Protein/Carbs/Fat ในฟอร์มรีวิว → ไม่ถูกนำไปตั้งเป็นเป้าหมายจริงของลูกเทรน กรอกแล้วหายไปเฉยๆ

**แก้แล้ว:** เรียก `setNutritionTarget()` ตอนกด "Reviewed" + toast บอกผลว่าอัปเดตสำเร็จหรือไม่

### 3. กันข้อมูล workout หายตอนเน็ตหลุด
**เดิม:** `saveWorkoutSession()` ถูกเรียกแบบไม่ `await` ไม่เช็คผล **4 จุด** รวมถึงตอน "ทำเสร็จ" — เน็ตหลุดในยิม (ชั้นใต้ดิน/ห้องปิด) = ข้อมูลหายเงียบ ไม่มีใครรู้

**แก้แล้ว:** เพิ่ม `persistSession()` — บันทึกลงเครื่องก่อนเสมอ ส่งขึ้น Firebase ไม่ผ่านให้ mark `syncStatus:"pending"` แล้ว **retry อัตโนมัติตอนเปิดหน้า Home** (pattern เดียวกับระบบโภชนาการที่พิสูจน์แล้ว)

### 4. แก้บั๊ก Dynamic Home ซ่อนการ์ดชวนออกกำลังกายถาวร
**เดิม:** เช็คสถานะจาก session ในเครื่องโดยไม่เทียบวันปฏิทิน — ลูกเทรนที่มีโปรแกรมเดียวในคิว **ทำเสร็จวันแรกแล้วจะไม่เห็นการ์ดชวนออกกำลังกายอีกเลยตลอดไป** ระบบกระโดดไปโชว์ "Perfect Day" ทั้งที่ยังไม่ได้ทำอะไรของวันนั้น

**แก้แล้ว:** ใช้ `member.workout.alreadyCompletedToday` (เทียบวันปฏิทินจาก Firebase) เป็นแหล่งความจริง

### 5. วันที่ใช้เวลาไทยทั้งระบบ
**เดิม:** `checkins.js` / `weekly-checkins.js` / `programs.js` ใช้ **UTC** ส่วน `nutrition.js` / `member-experience.js` ใช้ **เวลาเครื่อง** → ทดสอบยืนยันแล้วว่าข้อมูลที่บันทึกตอน **ตี 1 ของวันที่ 25** จะถูกเก็บเป็นวันที่ 24 ในระบบหนึ่ง และ 25 ในอีกระบบ (ชั่งน้ำหนักตอนเช้ามืดคือมาตรฐานที่ทุกคนทำ)

**แก้แล้ว:** ทั้ง 4 จุดใช้เวลาท้องถิ่นเหมือนกันหมด

### 6. Needs Attention เช็คความใหม่ของข้อมูลแล้ว
**เดิม:** เช็ค `workoutStatus !== "completed"` → คนหายไป 3 เดือน (session สุดท้าย completed) **ไม่ถูกแจ้งเตือนเลย** / คนกำลังออกกำลังกายอยู่ตอนนี้ (in_progress) **ถูกแจ้งเตือนผิดๆ**

**แก้แล้ว:** เช็คจาก "ไม่มีการฝึกมากี่วัน" (เกิน 6 วัน) + แยกข้อความ "ยังไม่เคยเริ่มโปรแกรม" ออกจาก "ไม่มีการฝึกมา X วัน" + เรียงตามความเร่งด่วน

### 7. แนวโน้ม 30 วันแสดงคู่กับเทียบเริ่มต้น
**เดิม:** `calculateChange()` เทียบ "ล่าสุด − ครั้งแรกสุด" เสมอ → คนที่ลด 90→75 kg แต่ 3 สัปดาห์หลังขึ้นมา 79 kg ยังเห็น "−11 kg" เขียวสวยตลอดไป เทรนเนอร์มองไม่เห็นสัญญาณเตือน

**แก้แล้ว:** เพิ่ม `calculateRecentChange()` แสดง 30 วันล่าสุดคู่กัน (หน้า Member Detail + Progress ฝั่งเทรนเนอร์)

---

## 🟠 Tier 2 — ลดแรงเสียดทาน

### 8. ปุ่ม "ข้ามท่านี้" พร้อมเหตุผล
**เดิม:** ไม่มีปุ่มข้ามเลย ระบบบังคับกรอก reps → ลูกเทรนที่ทำไม่ได้ต้องกรอก 0 (นับเป็น "ทำแล้ว" ทำให้สถิติเพี้ยน) หรือปล่อยค้าง (แยกไม่ออกจาก "เลิกกลางคัน")

**แก้แล้ว:**
- ปุ่ม "ข้ามท่านี้" + เลือกเหตุผลแตะเดียว 4 แบบ (เจ็บ / ไม่มีอุปกรณ์ / เครื่องไม่ว่าง / อื่นๆ)
- สถานะเซตแยกเป็น 3 แบบ: ทำแล้ว / ข้าม / ยังไม่ทำ
- **% ความสำเร็จคิดจากเซตที่ตั้งใจทำจริง ไม่รวมที่ข้าม** (ทดสอบแล้ว: ทำครบ 3/3 + ข้ามอีกท่า = 100% ไม่ใช่ 50%)
- เทรนเนอร์เห็นเหตุผลที่ข้าม → รู้ทันทีว่าต้องเปลี่ยนท่าให้ ไม่ใช่คิดว่าลูกเทรนขี้เกียจ

### 9. เลือกท่าได้หลายท่าต่อเนื่อง
**เดิม:** modal ปิดทันทีหลังเลือก 1 ท่า → โปรแกรม 8 ท่า = เปิด-ค้นหา-ปิด 8 รอบ

**แก้แล้ว:** modal เปิดค้าง เลือกต่อเนื่องได้ · ท่าที่เลือกแล้วขึ้น "เพิ่มแล้ว ✓" · ตัวนับบอกจำนวน · ปิดเองเมื่อกด "เสร็จสิ้น"

### 10. หน่วยสัดส่วน cm → นิ้ว
**เดิม:** ทั้ง 5 จุดวัด (เอว/อก/สะโพก/แขน/ต้นขา) ติดป้าย "cm" แต่คนไทยกรอกเป็นนิ้ว → ที่เห็น "รอบเอว 32 cm" ในหน้าจอ ไม่ใช่ผู้ใช้กรอกผิด แต่**แอปติดป้ายผิด**

**แก้แล้ว:** เปลี่ยนเป็น "นิ้ว" ทุกหน้า (น้ำหนัก kg / Body Fat % คงเดิม) · **ไม่ต้องแปลงข้อมูลเก่า** เพราะผู้ใช้กรอกเป็นนิ้วมาตั้งแต่ต้นแล้ว

### 11. จำกัดค่าสูงสุดต่อช่อง
**เดิม:** มีแค่ `min="0"` → รับค่า Body Fat 165% ได้ (เห็นในหน้าจอจริง) แล้วเอาไปคำนวณสรุปให้เทรนเนอร์ดู

**แก้แล้ว:** น้ำหนัก ≤300 · Body Fat ≤60 · กล้ามเนื้อ ≤100 · สัดส่วน ≤50-100 ตามจุด

### 12. ปุ่ม ⋮ เห็นชัดขึ้น
**เดิม:** ตัวเล็ก สีจาง ไม่มีพื้นหลัง — **คนสร้างแอปเองยังหาปุ่มลบสมาชิกไม่เจอ**

**แก้แล้ว:** วงกลม 40×40 มีเส้นขอบและพื้นหลัง ดูออกว่ากดได้

### 13. ปุ่ม "เลิกทำ" หลังลบคิวโปรแกรม
**แก้แล้ว:** Toast ค้าง 6 วินาทีพร้อมปุ่มเลิกทำ ทั้งตอนลบ Day เดียวและล้างคิวทั้งหมด (เก็บ snapshot ก่อนลบ)

> **หมายเหตุ:** ไม่ได้ทำ undo สำหรับ "ลบสมาชิก" เพราะการลบจะลบ check-in/session/รูปทั้งหมดด้วย การกู้คืนแค่ระเบียนสมาชิกจะให้ความมั่นใจผิดๆ ว่าข้อมูลกลับมาครบ — ยังใช้การยืนยันด้วยการพิมพ์ชื่อเหมือนเดิม

### 14. Loading state 4 หน้าฝั่งเทรนเนอร์
**แก้แล้ว:** `weekly-checkin-page` · `exercise-library-page` · `program-builder` · `progress-page` (เดิมจอค้างว่างเปล่าระหว่างโหลด)

### 15. Weekly Check-in ตัดช่องซ้ำซ้อน + คำนวณอัตโนมัติ
**เดิม:** 9 ช่อง โดย 3 ช่องระบบรู้อยู่แล้ว (น้ำหนัก, Workout %, Nutrition %) ให้ลูกเทรน**เดาเปอร์เซ็นต์เอง**

**แก้แล้ว:**
- Workout % คำนวณจาก session ที่ทำสำเร็จ เทียบจำนวน Day ในคิว
- Nutrition % คำนวณจากจำนวนวันที่บันทึกอาหาร ใน 7 วันล่าสุด
- น้ำหนักดึงค่าล่าสุดมาแสดง (ไม่ต้องกรอกซ้ำ) + ปุ่มลิงก์ไปบันทึกถ้ายังไม่มี
- แสดงเป็นกล่อง "ระบบสรุปให้อัตโนมัติ" ให้เห็นว่าเลขมาจากไหน
- **เหลือ 5 ช่องที่ต้องกรอกจริง** (นอน/เครียด/พลังงาน/ความหิว + ข้อความ)

---

## 🟡 Tier 3 — ประสบการณ์แบบแอปฟิตเนส

### 16. กราฟความก้าวหน้าในหน้า Progress ของลูกเทรน
**เดิม:** ไม่มีกราฟเลย มีแค่ตัวเลข — ทั้งที่ `drawLineChart()` มีอยู่แล้วและใช้งานได้ (ใช้แค่ฝั่งเทรนเนอร์)

**แก้แล้ว:** กราฟเส้นพร้อมแท็บสลับ น้ำหนัก / Body Fat / รอบเอว

### 17. RPE และ Volume แสดงให้เทรนเนอร์เห็นแล้ว
**เดิม:** ลูกเทรนกรอก RPE **ทุกเซต ทุกท่า ทุกวัน** (วันละ 20+ ครั้ง) แต่ **ไม่ถูกแสดงที่ไหนเลยแม้แต่จุดเดียว** และไม่มีการคำนวณ Volume ทั้งที่เป็นตัวชี้วัดหลักของการเทรนแรง

**แก้แล้ว:** หน้ารายละเอียดวันแสดง RPE ต่อเซต · RPE เฉลี่ยต่อท่า · **Volume (น้ำหนัก × ครั้ง)** · จำนวนเซตที่ข้าม

### 18. น้ำหนักครั้งก่อนแสดงตอนเล่น (progressive overload)
**เดิม:** ค่าเริ่มต้นมาจากโปรแกรมที่เทรนเนอร์ตั้งไว้ตายตัว → คนที่พัฒนา 20→30→40 kg **ยังเห็น 20 kg เป็นค่าเริ่มต้นทุกครั้งตลอดไป**

**แก้แล้ว:** ดึงน้ำหนักที่ยกได้จริงครั้งล่าสุดของแต่ละท่ามาเป็นค่าเริ่มต้น + แสดงข้อความ "ครั้งก่อนยกได้ X kg"

### 19. Day N indicator
**แก้แล้ว:** แสดง "DAY 2/3" บนหัวหน้า Workout และการ์ด Workout บนหน้า Home

### 20. Streak วันติดต่อกัน
**แก้แล้ว:** Badge "🔥 X วันติดต่อกัน" บนหน้า Home (แสดงเมื่อ ≥2 วัน) · ทดสอบแล้ว: ทำ 3 วันติด = 3 · ขาดไป 3 วัน = 0

### 21. การ์ดสรุปโชว์ครบทุกครั้ง (ดีไซน์ใหม่ Home)
**เดิม:** การ์ดข้อมูลชุดล่างจะ**ถูกซ่อน**ถ้าซ้ำกับการ์ดเด่นด้านบน → ผู้ใช้งงว่าทำไมบางวันการ์ดหาย

**แก้แล้ว:** Workout / Nutrition / Weight โชว์ครบทุกครั้ง ไม่มีเงื่อนไขซ่อน (ตามแบบ A ที่เลือกไว้)

### 22. Animation แบบ minimal
**เดิม:** CSS 4,692 บรรทัด มี animation แค่ 2 จุด — แอปนิ่งสนิท เปลี่ยนแบบกระตุก

**แก้แล้ว:** page fade-in 150ms · pop ตอนติ๊กเซตสำเร็จ · toast fade
**และเพิ่ม `prefers-reduced-motion`** (เดิมไม่มีเลย) — คนที่ตั้งค่าปิด animation ในเครื่องจะไม่เห็นการเคลื่อนไหวใดๆ

---

## 🤖 AI — เพิ่มประโยชน์โดยคุมเครดิต

### 23. เพิ่มลิมิตวิเคราะห์รูปอาหาร 3 → 5 ครั้ง/คน/วัน
คงลิมิตรวมทั้งโปรเจกต์ไว้ **60/วัน** ตามที่ต้องการ

> **ข้อควรทราบ:** ด้วยตัวเลขนี้ ลูกเทรน 12 คนที่ใช้เต็มโควต้าจะกินลิมิตรวมหมด คนที่เหลือจะใช้ไม่ได้จนเที่ยงคืน

### 24. ระบบเตือนสัญญาณผิดปกติ — **ไม่ใช้ AI เลย ไม่มีค่าใช้จ่าย**
ไฟล์ใหม่ `anomaly-alerts.js` ใช้คณิตศาสตร์ล้วน ตรวจ 3 สัญญาณ:
- **น้ำหนักลดเร็วเกิน 1.2 kg/สัปดาห์** ต่อเนื่อง → "ควรตรวจว่ากินพอไหม"
- **บันทึกอาหารลดลงเกินครึ่ง** → "อาจกำลังถอย"
- **ทำ Workout ไม่ถึงครึ่งของแผน 2 สัปดาห์ติด** → "ควรคุยเรื่องอุปสรรค"

แสดงเป็นการ์ดเตือนบน Dashboard (ระบบแค่สะกิดให้ดู ไม่ตัดสินใจหรือแก้ข้อมูลเอง)
ทดสอบแล้วทั้ง 3 ตัวตรวจจับถูกต้อง และเคสปกติไม่เตือนผิด

---

## 🟢 Tier 4 — ความสะอาดของโค้ด

### 25. ลบไฟล์ตาย ~330 บรรทัด
`trainer-dashboard.js` (213 บรรทัด, เวอร์ชันเก่าซ้ำซ้อน) · `trainer.js` (111) · `trainer-notes.js` · `member-history.js`
**และเอาออกจาก `sw.js`** — เดิมผู้ใช้ทุกคนดาวน์โหลดโค้ดที่ไม่ทำงานทุกครั้งที่อัปเดต

### 26. ลบตัวแปร CSS ซ้ำ 11 ตัว
`base.css` และ `design-system.css` ประกาศ `--primary`, `--text`, `--surface` ฯลฯ ซ้ำกันคนละค่า → design-system โหลดทีหลังจึงชนะเสมอ **ตัวแปรใน base.css ตายหมด** ใครแก้สีตรงนั้นจะงงว่าทำไมไม่เปลี่ยน
**แก้แล้ว:** เหลือแหล่งเดียวคือ `design-system.css`

### 27. ลบ routing เก่าที่ unreachable ใน app.js
regex matching ~60 บรรทัดใน `registerRoute("/404")` ที่ไม่มีทางทำงาน (ทุก route ลงทะเบียนไว้ก่อนหน้าแล้ว)

### 28. แก้ N+1 query ในหน้าภาพรวม Workout
เดิมยิง 2 คำขอต่อสมาชิก 1 คน (30 คน = 60 คำขอ) → เปลี่ยนไปใช้ `getWorkoutSessions()` ดึงครั้งเดียว
*(บั๊กนี้ผมสร้างขึ้นเองตอนสร้างหน้านี้ให้ก่อนหน้านี้)*

### 29. เพิ่ม aria-label ปุ่มไอคอน
ปุ่มย้อนกลับและปุ่มปิด modal ที่ยังไม่มี (screen reader อ่านไม่ออก)

---

## ✅ การตรวจสอบ
- JS syntax ผ่านทั้ง **52 ไฟล์** + `sw.js`
- CSS braces สมดุลทั้ง 5 ไฟล์
- `import` ทุกตัวชี้ไปไฟล์ที่มีอยู่จริง (ตรวจอัตโนมัติหลังลบไฟล์ตาย)
- ไฟล์ทั้ง 60 รายการใน `sw.js` cache มีอยู่จริง
- ทดสอบ logic ด้วยข้อมูลจำลอง: streak · % ความสำเร็จเมื่อมีเซตข้าม · ระบบเตือน 3 ตัว · การคำนวณวันที่ timezone

---

## ⏳ ยังเหลือ (Batch 2)

| รายการ | เหตุผลที่ยังไม่ทำ |
|---|---|
| PR อัตโนมัติ + ให้ลูกเทรนดูได้ | ต้องออกแบบว่าจะนับ PR จากอะไร (1RM ประมาณ / น้ำหนักสูงสุด / volume สูงสุด) |
| เทียบรูปก่อน-หลัง 2 วัน | งาน UI ใหม่ทั้งหน้า |
| น้ำหนักแหล่งเดียว (ยุบ 2 path ใน Firebase) | ต้องย้ายข้อมูลเก่า ควรทำแยกและทดสอบละเอียด |
| ยุบ 8 หน้าย่อยเป็น 4 แท็บ | เป็นการรื้อ navigation ใหญ่ ควรทำเป็นชุดเดียว |
| AI ช่วยร่างคำติชม | ต้องออกแบบ prompt + ระบบ cache ต่อ check-in ก่อน |
| ระบบแจ้งอาการเจ็บด่วน | ต่อยอดจากเหตุผล "เจ็บ" ในปุ่มข้ามท่าที่ทำไว้แล้ว |
| ติดตามรอบเดือน (opt-in) | ต้องคุยเรื่องความเป็นส่วนตัวและการเปิด/ปิด |
| Rest timer แบบวงแหวน | ต้องเขียน SVG ring ใหม่ |
| aria-label ทุกปุ่มทั้งระบบ | ทำเฉพาะปุ่มที่แก้ไปในรอบนี้ |

---

## วิธี deploy
แทนที่ทั้งโฟลเดอร์ (มีการ **ลบไฟล์** 4 ไฟล์ และ **เพิ่มใหม่** 1 ไฟล์ `js/anomaly-alerts.js`)
Cache version bump แล้ว → ผู้ใช้จะได้เวอร์ชันใหม่อัตโนมัติ

**สิ่งที่ควรทดสอบก่อนใช้จริง:**
1. เข้าหน้า Weekly ของลูกเทรน ดูว่าเห็นคำติชมที่โค้ชเขียนไว้
2. โค้ชรีวิว + กรอก Calories → เช็คว่าเป้าโภชนาการของลูกเทรนอัปเดตจริง
3. กดข้ามท่า → ดูว่า % ความสำเร็จไม่เพี้ยน และเทรนเนอร์เห็นเหตุผล
4. ปิด wifi แล้วบันทึกเซต → เปิด wifi กลับมาเข้าหน้า Home → เช็คว่าข้อมูลขึ้น Firebase
5. ทำ workout เสร็จวันนี้ → **พรุ่งนี้เช้าเปิดหน้า Home** เช็คว่าการ์ดชวนออกกำลังกายกลับมา

---
---

# v3 Batch 2

Cache version: `morning-warrior-v3-batch2`

## 30. PR ตรวจจับอัตโนมัติ + ลูกเทรนดูได้เอง
**เดิม:** ระบบ `prs.js` มีอยู่แล้วแต่ต้อง**กรอกด้วยมือ** และแสดงเฉพาะฝั่งเทรนเนอร์ ลูกเทรนดูสถิติตัวเองไม่ได้เลย

**แก้แล้ว:**
- เพิ่ม `findNewPR()` + `recordAutoPR()` — เทียบน้ำหนักที่เพิ่งบันทึกกับสถิติสูงสุดเดิมของท่านั้นอัตโนมัติ
- ทำได้เกินเดิม → ป๊อปอัพ **"New PR! 🎉"** ขึ้นทันที พร้อมบอกสถิติเดิมเทียบให้เห็น
- ลูกเทรนดู PR ตัวเองได้ในหน้า Progress (section ใหม่ แสดง 6 ท่าที่หนักสุด)
- ทดสอบแล้ว: ยกเกินเดิม = PR ✓ / ยกต่ำกว่าเดิม = ไม่ใช่ ✓ / ท่าใหม่ = PR ✓ / น้ำหนัก 0 = ไม่ใช่ ✓

> ระบบแค่บันทึกและแจ้งให้รู้ ไม่ได้ไปแก้โปรแกรมหรือน้ำหนักเป้าหมายอะไรเอง

## 31. Rest Timer เป็นวงแหวนนับถอยหลัง
**เดิม:** เป็นแค่ตัวเลขเปลี่ยนวูบๆ ทุกวินาที ทั้งที่เป็นหน้าที่ลูกเทรน**จ้องบ่อยที่สุดในแอป** (พักระหว่างเซตทุกครั้ง)

**แก้แล้ว:** วงแหวน SVG ค่อยๆ ลดลงตามเวลาจริง + ตัวเลขกลางวง (แบบ Apple Watch) · มี `prefers-reduced-motion` override

## 32. ระบบแจ้งเตือนอาการเจ็บถึงเทรนเนอร์
**ต่อยอดจากปุ่มข้ามท่าใน Batch 1** — ไม่ต้องให้ลูกเทรนกรอกอะไรเพิ่มเลย

เมื่อลูกเทรนข้ามท่าโดยเลือกเหตุผล **"เจ็บ/ไม่สบายตัว"** → ขึ้นการ์ดเตือน**สีแดง (danger)** บน Dashboard ทันที บอกว่าข้ามท่าอะไรไป และ**เรียงขึ้นก่อนสัญญาณอื่นทั้งหมด** เพราะเป็นประเด็นความปลอดภัย

ทดสอบแล้ว: เจ็บ 2 ท่าใน 7 วัน = เตือน ✓ / ข้ามเพราะไม่มีอุปกรณ์ = ไม่เตือน ✓ / เจ็บเมื่อ 30 วันก่อน = ไม่เตือน ✓

## 33. เทียบรูปก่อน-หลัง
**เดิม:** รูปแต่ละชุดแสดงแยกเป็นบล็อกตามวันที่ ต้องเลื่อนดูสองที่เอง

**แก้แล้ว:** โหมด "เทียบก่อน–หลัง" ในหน้ารูปฝั่งเทรนเนอร์ — เลือก 2 วันจาก dropdown แล้ววางคู่กันทีละมุม (หน้า/ข้าง/หลัง) · ค่าเริ่มต้นคือชุดแรกสุด vs ชุดล่าสุด · แสดงเฉพาะเมื่อมีรูป ≥2 ชุด

## 34. AI ช่วยร่างคำติชม — คุมเครดิตเข้ม
ไฟล์ใหม่ `ai-coach-draft.js`

**กลไกประหยัดเครดิตตามที่ตกลงกันไว้:**
1. **กดปุ่มเรียกเองเท่านั้น** — ไม่มี auto-generate ตอนเปิดหน้า
2. **Cache ต่อ check-in** — กดซ้ำใช้ร่างเดิม ไม่เสียเครดิตเพิ่ม (toast บอกชัดว่า "ไม่เสียเครดิตเพิ่ม")
3. ถ้ามีข้อความอยู่แล้ว **ถามยืนยันก่อนเขียนทับ**

**สิ่งที่ AI ทำ:** อ่านข้อมูลจริงของสัปดาห์นั้น (adherence, การนอน, ความเครียด, พลังงาน, น้ำหนัก, สิ่งที่ทำได้ดี, อุปสรรค, คำถามจากลูกเทรน) แล้วร่าง `feedback` + `nextWeekGoal` เป็นภาษาไทย

**ข้อจำกัดที่ใส่ไว้ใน prompt:** ห้ามแต่งตัวเลขเพิ่ม · ห้ามให้คำแนะนำทางการแพทย์ (ถ้าเจอสัญญาณบาดเจ็บให้แนะนำคุยกับเทรนเนอร์/แพทย์) · ระบุชัดว่าเป็นเพียงร่าง

**ใต้ปุ่มมีข้อความกำกับ:** "AI จะร่างจากข้อมูลจริงของสัปดาห์นี้ · คุณต้องตรวจและแก้ก่อนส่งเสมอ"

---

## ✅ การตรวจสอบ Batch 2
- JS syntax ผ่าน **53 ไฟล์** + `sw.js`
- CSS braces สมดุลทั้ง 5 ไฟล์
- `import` ทุกตัว resolve ได้
- ไฟล์ 61 รายการใน `sw.js` cache มีอยู่จริงครบ
- ทดสอบ logic ด้วยข้อมูลจำลอง: PR detection (4 เคส) · injury detection (3 เคส)

## ⏳ ยังเหลือ (Batch 3)

| รายการ | เหตุผล |
|---|---|
| น้ำหนักแหล่งเดียว (ยุบ 2 Firebase path) | ต้องย้ายข้อมูลเก่า ควรทำแยกและทดสอบละเอียด — เสี่ยงข้อมูลหายถ้าพลาด |
| ยุบ 8 หน้าย่อยเป็น 4 แท็บ | รื้อ navigation ทั้งระบบ ควรทำเป็นชุดเดียวและทดสอบทุกเส้นทาง |
| ติดตามรอบเดือน (opt-in) | ต้องคุยเรื่องความเป็นส่วนตัวและวิธีเปิด/ปิดก่อน |
| aria-label ทุกปุ่มทั้งระบบ | ทำเฉพาะปุ่มที่แตะในรอบนี้ ยังเหลือหลายจุด |
| กราฟ Volume รายสัปดาห์ | Volume ต่อ session ทำแล้ว แต่กราฟเทรนด์ยังไม่ได้ทำ |
| AI ตอบคำถามลูกเทรน / แนะนำท่าทดแทน | ชะลอไว้ตามที่ตกลง — คาดเดาปริมาณเครดิตไม่ได้ |

## วิธี deploy
แทนที่ทั้งโฟลเดอร์ · ไฟล์ใหม่ใน Batch 2: `js/ai-coach-draft.js`
Cache version → `morning-warrior-v3-batch2`

**ทดสอบเพิ่มสำหรับ Batch 2:**
1. บันทึกเซตด้วยน้ำหนักที่มากกว่าเดิม → เช็คว่าขึ้น "New PR! 🎉"
2. ดูหน้า Progress ลูกเทรน → เช็คว่าเห็นกราฟและ section Personal Records
3. กดข้ามท่าเลือกเหตุผล "เจ็บ" → เช็คว่า Dashboard เทรนเนอร์ขึ้นการ์ดแดงทันที
4. กด "ให้ AI ช่วยร่างคำติชม" 2 ครั้ง → ครั้งที่ 2 ต้องขึ้น "ไม่เสียเครดิตเพิ่ม"
5. หน้ารูปฝั่งเทรนเนอร์ (ต้องมีรูป ≥2 ชุด) → เช็คโหมดเทียบก่อน-หลัง
